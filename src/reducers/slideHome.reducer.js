import dataSLideHome from '../Data/slideHome';

const initialState = {
    items: dataSLideHome
};

export default (state = initialState, action ={}) =>  {
    switch(action.type) {
        default: 
        return state
    };
};